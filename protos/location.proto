syntax = "proto3";
package v1;

message Coordinates {
  string ip = 1;
  int32 x = 2;
  int32 y = 3;
  
}

service LocationDelivery {

    rpc Deliver (stream Coordinates) returns (stream Coordinates) {}

}
